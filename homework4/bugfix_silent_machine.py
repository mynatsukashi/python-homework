def add(a, b):
    # Print doesn't return any value, it displays it in terminal and that's it. Meanwhile, Return displays, store its value and uses whenever we call it.
    return(a + b)

total = add(10, 5) + 100
print(f"{total = }")